apiVersion: 1

# Джерело даних — PostgreSQL сервісу Store (таблиця processed_agent_data)
datasources:
  - name: RoadVision PostgreSQL
    uid: roadvision-pg
    type: grafana-postgresql-datasource
    access: proxy
    url: postgres_db:5432
    user: user
    isDefault: true
    jsonData:
      database: test_db
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    secureJsonData:
      password: pass
